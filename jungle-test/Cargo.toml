[package]
name = "jungle-test"
version = "0.1.0"
edition = "2021"

[dependencies]
jungle-sdk = { path = "../jungle-sdk" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
