Done. Moved the `Entity` trait from `jungle-sdk/src/lib.rs` into `jungle-core/src/lib.rs` and added a `pub use jungle_core::Entity` re-export in `jungle-sdk` for backwards compatibility. All workspace members compile cleanly.