Done — added `typosaurus` as a workspace dependency in the root `Cargo.toml` with `path = "../typosaurus"`, and consumed it in `jungle-types/Cargo.toml` via `typosaurus = { workspace = true }`. Workspace builds cleanly.