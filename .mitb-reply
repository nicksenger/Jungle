Done — added `jungle-types` as a new workspace member. Created the crate at `jungle-types/` with a standard `Cargo.toml` and `src/lib.rs`, and added it to the workspace members list. Workspace builds cleanly.