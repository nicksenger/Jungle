Done — `Action` and `Impulse` have been moved into a new `behavior.rs` module. The `lib.rs` now declares `mod behavior` and re-exports both traits via `pub use behavior::{Action, Impulse}`, so existing `use jungle_sdk::Action` / `use jungle_sdk::Impulse` imports remain unchanged.