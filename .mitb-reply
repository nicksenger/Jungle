Done — `Genus` and `Species` have been moved into a new `taxonomy.rs` module. The `lib.rs` now declares `mod taxonomy` and re-exports both traits via `pub use taxonomy::{Genus, Species}`, so existing `use jungle_sdk::Genus` / `use jungle_sdk::Species` imports remain unchanged.